[project]
name = "frameworks"
version = "0.1.0"
description = "Pipeline per censire i framework usati dalle homepage degli enti in enti.csv"
requires-python = ">=3.13"
readme = "README.md"
dependencies = [
    "playwright>=1.59.0",
]
